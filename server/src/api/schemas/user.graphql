type User {
  id: ID!
  user_name: String!
  email: String!
  password: String!
}

input UserInput {
  user_name: String!
  email: String!
  password: String!
}

type Query {
  users: [User],
  user(id: ID!): User,
}

type Mutation {
  createUser(input: UserInput!): User
}
